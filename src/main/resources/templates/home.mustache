{{>header}}
    <div>
        <h1>{{hi}}</h1>
    </div>
    <div class="col-md-12">
        <div class="row">
            <div class="col-md-6">
                {{#user}}
                Logged in as: <span id="user">{{user.name}}</span>
                <img src={{user.picture}}>
                <a href="/logout" class="btn btn-info active" role="button">Logout</a>
                {{/user}}
                {{^user}}
                    <a href="/oauth2/authorization/google" class="btn btn-success acitve"
                       role="button">Google Login</a>
                {{/user}}

            </div>
            <div class="col-md-6">
                <a href="/list" role="button">목록</a>
            </div>
            <div>
                <a href="/diary-post" class="btn btn-info active" role="button">Diary 작성하기</a>
            </div>
        </div>
        <br>
        <!-- 목록 출력 영역 -->
    </div>
{{>footer}}